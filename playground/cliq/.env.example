# Cliq Participation Handler PoC — environment template.
# Copy to .env and fill in:  cp .env.example .env

# Shared secret used to HMAC-SHA256 sign Deluge → receiver requests.
# The same value goes into the bot's WEBHOOK_SECRET variable in Cliq.
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=
